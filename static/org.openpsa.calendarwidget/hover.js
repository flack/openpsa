function org_openpsa_calendarwidget_month_show_box(id)
{
    // A bit of delay

    box = document.getElementById(id);
    box.style.display = 'block';
}

function org_openpsa_calendarwidget_month_hide_box(id)
{
    box = document.getElementById(id);
    box.style.display = 'none';
}